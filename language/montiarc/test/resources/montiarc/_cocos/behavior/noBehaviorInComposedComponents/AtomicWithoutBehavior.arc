/* (c) https://github.com/MontiCore/monticore */
package behavior.noBehaviorInComposedComponents;

// Valid, there is no behavior, so it can't be wrong
component AtomicWithoutBehavior {
  port in int number;

}