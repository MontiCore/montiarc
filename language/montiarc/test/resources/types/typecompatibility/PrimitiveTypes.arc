/* (c) https://github.com/MontiCore/monticore */
package types.typecompatibility;

/*
 * Valid model.
 */
component PrimitiveTypes {

    port in int inInt;
    port in double inDouble;
    port in float inFloat;
    port in boolean inBoolean;
    port in char inChar;
    port in byte inByte;
    port in long inLong;
    port in short inShort;
    port in String inString;

}