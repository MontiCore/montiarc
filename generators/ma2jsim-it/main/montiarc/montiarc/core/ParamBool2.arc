/* (c) https://github.com/MontiCore/monticore */
package montiarc.core;

component ParamBool2(boolean p) {

  port in boolean i;
  port out boolean o;

  // boolean v = p;

  automaton {
    initial state S;
  }
}
