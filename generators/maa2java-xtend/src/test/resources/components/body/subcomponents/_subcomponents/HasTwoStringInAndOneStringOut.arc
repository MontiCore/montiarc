package components.body.subcomponents._subcomponents;

/*
 * Valid model.
 * Formerly named 'C'
 */
component HasTwoStringInAndOneStringOut {
    port
        in String sIn1,
        in String sIn2,
        out String sOut;
}