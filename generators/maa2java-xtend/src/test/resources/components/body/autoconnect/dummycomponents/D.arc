package components.body.autoconnect.dummycomponents;

component D {
    port 
        in String dataSthElse,
        in Integer myInt,
        in Boolean bool,
        out String strOut,
        out String sthElse,
        out Integer intOut;
        
}