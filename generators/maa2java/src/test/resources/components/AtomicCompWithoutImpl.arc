package components;

/*
 * Valid model.
 * Used as the first test model to implement the basic generator test framework.
 */
component AtomicCompWithoutImpl(Integer test){

  port in List<Integer> inIntEmpty;
  port out String outString;

}