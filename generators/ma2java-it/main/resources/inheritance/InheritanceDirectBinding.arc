/* (c) https://github.com/MontiCore/monticore */
package inheritance;

/**
 * Component that extends another component and directly binds the first argument
 */
component InheritanceDirectBinding(int x) extends Parent(10, x) {
}