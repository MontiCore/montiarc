/* (c) https://github.com/MontiCore/monticore */
package light;

classdiagram Datatypes {
  enum SwitchStatus {On, Off;}
  enum DoorStatus {Open, Closed;}
  enum OnOffCmd {On, Off;}
  enum AlarmStatus {On, Off;}
  enum BlinkRequest {On, Off;}
}