package components.body.subcomponents;

/*
 * Valid model with warnings.
 * Uses implicit and explicit
 *
 * Formerly named "E22" in MontiArc3.
 *
 * @implements [Hab16] CV7: Avoid using implicit and explicit names for
 *                            elements with the same type. (p. 72, Lst. 3.54)
 * TODO Add Test
 */
component SubcomponentsWithImplicitAndExplicitNames {
  component components.body.subcomponents._subcomponents.AtomicComponent named;
  component components.body.subcomponents._subcomponents.AtomicComponent;
      // Warning: Implicit naming should be used for unique subcomponent types only
}