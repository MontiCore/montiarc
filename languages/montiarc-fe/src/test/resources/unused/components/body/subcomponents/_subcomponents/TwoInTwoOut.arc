package components._subcomponents;

/*
 * Valid model.
 * Original Name: TwoInTwoOut
 */
component TwoInTwoOut {
    
    port
        in String stringIn,
        in Integer intIn,
        out String stringOut,
        out Integer intOut;
}