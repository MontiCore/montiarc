package components.body.ports;

import java.lang.Integer;

/*
 * Valid model with warnings.
 * Uses implicit and explicit
 *
 * Formerly named "E11" in MontiArc3.
 *
 * @implements [Hab16] CV7: Avoid using implicit and explicit names for
 *                            elements with the same type. (p. 72, Lst. 3.54)
 * TODO Add Test
 */
component ImplicitAndExplicitPortNaming2 {
 port 
   in Integer, //WARNING: Implicit naming should be used for unique port types only
   out Integer someIntName;
}