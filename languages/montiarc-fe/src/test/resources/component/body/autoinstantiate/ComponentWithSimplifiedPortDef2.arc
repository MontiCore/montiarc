package component.body.autoinstantiate;

/** 
 * Valid model.
 */
component ComponentWithSimplifiedPortDef2 {
  port 
    in int a, b, c;
    
  port 
    in String s1, s2;
    
  port 
    out int d;
}