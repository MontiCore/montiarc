package cg1;

/*
 * TODO Invalid?
 * From old tests:
 *    The type 'myTypes' cannot be loaded!
 *    The configuration file 'conf.extern' cannot be loaded!
 *    The type 'impl.NotExisting' cannot be loaded!
 */
component Init {

  component Ref someName;
  
  component Ref2 anotherName;
}