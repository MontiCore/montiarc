package components.head.parameters;

/*
 * Invalid model.
 *
 * @implement [Hab16] CV2: Types start with an upper-case letter. (pg. 71, Lst. 3.51)
 * Component types and generic type parameters should start with an upper case letter.
 */
component ComponentWithLowerCaseGenType<K, t> {
    
    
}