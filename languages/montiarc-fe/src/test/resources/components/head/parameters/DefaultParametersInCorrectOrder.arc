/* (c) https://github.com/MontiCore/monticore */
package components.head.parameters;

/**
 * Valid model. No normal parameters follow after the first default
 * parameter.
 */ 
component DefaultParametersInCorrectOrder(int x, int y = 5) 
{
}
