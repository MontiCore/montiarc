package components.body.automaton;

/**
 * Invalid model. Automata without states are prohibited.
 * @implements [Wor16] AC3: The automaton has at least one state. (p. 99, Lst. 5.13)
 */
component AutomatonWithoutState {

  automaton AutomatonWithoutState {
  }
}
