package components.body.variables;


/**
* Invalid model: variable names have to start with lowercase letter.
* @implements [RRW14a] C2: The names of variables and ports start with lowercase letters. (p. 31, Lst. 6.5)
*
*/
component VariableNameUpperCase {

  String X;
}