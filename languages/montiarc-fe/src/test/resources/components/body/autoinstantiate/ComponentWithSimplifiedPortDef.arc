/* (c) https://github.com/MontiCore/monticore */
package components.body.autoinstantiate;

/** 
 * Valid model.
 */
component ComponentWithSimplifiedPortDef {
  port
    in int a, b, c,
    in String s1, s2,
    out int d;
}
