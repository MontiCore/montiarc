package components.body.ports;

/**
 * Invalid model.
 * @implements PortNamesAreNotJavaKeywords
 */
component KeywordAsPortName {
  
  port in char,
   out String goto,
   in Int<int>,
   out Class;
   
  
   
}