package components.body.subcomponents._subcomponents;

import components.body.subcomponents._subcomponents.HasGenericOutput;

/*
 * Valid model.
 */
component InheritsOutgoingStringPort extends HasGenericOutput<String> {
  // Empty body
}
