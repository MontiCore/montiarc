package components.body.subcomponents._subcomponents;

/*
 * Valid model.
 * Used by SubcomponentGenericTypesCorrectlyAssigned.
 */
component SimpleGenericComponent<T,K> {
  port in String stringIn;
}