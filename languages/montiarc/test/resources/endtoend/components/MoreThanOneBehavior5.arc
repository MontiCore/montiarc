/* (c) https://github.com/MontiCore/monticore */
package components;

/**
 * Invalid model: The component has three automata (more than one behavior description).
 */
component MoreThanOneBehavior5 {

  automaton { initial state S; }
  automaton { initial state S; }
  automaton { initial state S; }

}
