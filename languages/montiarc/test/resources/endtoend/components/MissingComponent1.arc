/* (c) https://github.com/MontiCore/monticore */
package components;

/**
 * Invalid model: The type Missing (simple name) of subcomponent sub is missing
 * (the component cannot be resolved).
 */
component MissingComponent1 {
  Missing sub;
}
