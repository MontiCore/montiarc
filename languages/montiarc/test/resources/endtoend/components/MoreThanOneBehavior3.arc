/* (c) https://github.com/MontiCore/monticore */
package components;

/**
 * Invalid model: The component has an automaton and a compute block (more than
 * one behavior description).
 */
component MoreThanOneBehavior3 {

  automaton { initial state S; }
  compute { }

}
