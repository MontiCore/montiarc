/* (c) https://github.com/MontiCore/monticore */
package variability;

/*
 * Invalid model: The symbol 'e' referenced in the constraint is missing
 * (the symbol cannot be resolved).
 */
component MissingSymbolsInConstraint1 {

  constraint(e);

}
