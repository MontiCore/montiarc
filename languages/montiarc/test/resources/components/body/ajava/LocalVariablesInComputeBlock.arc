/* (c) https://github.com/MontiCore/monticore */
package components.body.ajava;

/*
 * Valid model.
 */
component LocalVariablesInComputeBlock{


  compute AJavaComputeBlock{
    int x = 0;
    for(int i = 0; i<10;i++) {

    }
  }

}