/* (c) https://github.com/MontiCore/monticore */
package originalStateChartCoCos;

component NoStatechart {
  port in boolean open,
       in boolean unlock;
  port out boolean ringing;


  // valid model
}
