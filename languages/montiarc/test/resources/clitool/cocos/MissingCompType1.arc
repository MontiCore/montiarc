/* (c) https://github.com/MontiCore/monticore */

/**
 * The model is invalid. The subcomponent's type (simple name) cannot be
 * resolved. The tool should report an error for the missing type.
 */
component MissingCompType1 {
  Missing sub;
}
