package java.util;

// TODO remove this class. Only needed because of generated datatype contain Optional import.
public class Optional {
  public Optional() {
    throw new RuntimeException("Not supported!");
  }
}
